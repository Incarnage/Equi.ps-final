import 'package:flutter/material.dart';

class LessorRandom extends StatelessWidget {
  const LessorRandom({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
